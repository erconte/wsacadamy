package variaveis;

import javax.swing.JOptionPane;

public class ManipulaString {
	public static void main(String[] args) {
		String email = JOptionPane.showInputDialog("Digite e-mail: ").toLowerCase();
		System.out.println("Original: " + email);
		System.out.println("Minuscula: " + email.toLowerCase());
		System.out.println("Maiusculo: " + email.toUpperCase());
		System.out.println("Tem arroba?: " + email.contains("@"));
		System.out.println("Posição do @: " + email.indexOf("@"));
		System.out.println("Qtde: " + email.length());
		System.out.println("Do segundo até o quinto: " + email.substring(1,5));
		System.out.println("Primeira metade: " + email.substring(0, email.length()/2));
		System.out.println("Antes do @: " + email.substring(0, email.indexOf("@")).toUpperCase());
		System.out.println("Antes do @: " + email.substring(1 + email.indexOf("@"), email.length()));
		System.out.println("Primeiro Caracter: " + email.toLowerCase().charAt(0)); //colocar o LowerCase antes do charAt
		
		/*
		 * Identificador (nome dos elementos que criamos: variáveis, classes, pacotes, etc...)
		 * - Regras: 
		 * 1° - Não começarás com números. Exemplo: 1berto (errado) h1berto (certo).
		 * 2° - Não utilizarás palavras reservadas. Exemplo: não criar uma variável chamada "class" "void" "public" "int" "double"
		 * 3° - Não farás uso de caracteres especiais. Sem acento, sem cedilha, sem &. Exemplo: n@me (errado), não utilize espaço.
		 * 
		 * - Padrões: 
		 * 1 - CamelCase: quando tem mais que uma palavra, da segunda palavra em diante tem que começar com letra maiúscula
		 * certo: dataNascimento, errado: datanascimento
		 * certo: data_nascimento
		 * 
		 * 2 - UML: é responsável por padronizar elementos da Orientação a Objetos
		 * - Toda classe deve começar com letra maiúscula
		 * - Toda variável deve começar com letra minuscula
		 * - Todo método deve começar com letra minuscula (main, print, parseInt...) e deve
		 * ser seguido de parenteses.
		 * 
		 * Xpto.xpto() - é um método
		 * xpto() - é uma função
		 * xpto - é uma variável
		 * Xpto - é uma classe
		 * 
		 */
		
		
		//e r i c k c o n t e @ o u t l o o k . c o m
		//0 1 2 3 4 5 ...
		
		
		
	}

}
